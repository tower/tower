class Coach.Generator.DSL
  injectIntoFile: (file, options, callback) ->
    
  readFile: (file) ->
  
  createFile: (file, data) ->
    
  removeFile: (file) ->
    
  createDirectory: (name) ->
    
  removeDirectory: (name) ->

module.exports = Coach.Generator.DSL
