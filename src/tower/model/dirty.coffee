# @mixin
Tower.Model.Dirty =
  InstanceMethods:
    attributeChanged: (name) ->

    attributeChange: (name) ->

    attributeWas: (name) ->

    resetAttribute: (name) ->

module.exports = Tower.Model.Dirty
