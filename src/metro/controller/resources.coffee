Metro.Controller.Resources =
  ClassMethods:
    # resource name: "user", collectionName: "users", className: "User"
    resource: (options = {}) ->
      @_resource = options

  _create: (callback) ->
    
  resource: ->
    
  collection: ->
    
  resourceClass: ->
    
  buildResource: ->
    
  createResource: ->
  
  updateResource: ->
    
  destroyResource: ->
    
  parent: ->
    
  endOfAssociationChain: ->
    
  associationChain: ->

module.exports = Metro.Controller.Resources
