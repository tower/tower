Tower.Package.register(function(){
    // Add information to the package.
    this.info({
        name: "tower-model",
        description: "",
        version: "0.5.0",
        author: "Tower"
    });

    this.dependencies([]);

});