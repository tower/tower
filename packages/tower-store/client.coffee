require './shared'
require './client/transport'
require './client/localStorage'

# If you want to sync with Ajax, you must set the data transporter
# (need to find a better way to do this automatically)
# Tower.StoreMemory.transport = Tower.StoreTransportAjax
