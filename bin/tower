#!/usr/bin/env node
var path = require('path');

//try {
//  require('fibers')
//} catch (error) {}

try {
  require('tower');
} catch (error) {
  require(path.resolve(__dirname, '../lib/tower'));
}

Tower.Command.run(process.argv);
